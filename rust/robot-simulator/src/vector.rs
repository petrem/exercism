//! Simple, two-dimensional vectors.
//! The term "arrow" is vague, but hopefully less misleading then Vector,
//! which could be confused both for a `Vec` or to a general, multi-dimensional vector.

struct Arrow {
}
